require("nvim-biscuits").setup {
  default_config = {
    cursor_line_only = true,
    max_length = 12,
    min_distance = 5,
    prefix_string = "  ",
  },
  language_config = {
    help = { disabled = true },
  },
}
